<!DOCTYPE html>
<html>
  <head>
    <title>AttendanceApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <style>
      body {
        font-family: sans-serif;
        background-color: #f0f4f8;
        margin: 0;
        padding: 0;
      }
      .navbar {
        background-color: #007acc;
        color: white;
        padding: 1rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .container {
        padding: 2rem;
      }
      .card {
        background: white;
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 1rem;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      }
      .card.success {
        border-left: 6px solid #4caf50;
      }
      .card.pending {
        border-left: 6px solid #ff9800;
      }
      button {
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 8px;
        margin-right: 1rem;
        font-size: 1rem;
        cursor: pointer;
      }
      .btn-disabled {
        background-color: #ccc;
        color: #666;
        cursor: not-allowed;
      }
      .btn-active {
        background-color: #007acc;
        color: white;
      }
      .footer-button {
        margin-top: 2rem;
        display: block;
        background-color: #007acc;
        color: white;
        text-align: center;
        padding: 1rem;
        border-radius: 10px;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="navbar">
      <div>勤怠管理システム</div>
      <div>田中さん ｜ <a href="#" style="color: white; text-decoration: underline;">ログアウト</a></div>
    </div>
    <div class="container">
      <h2>おはようございます、田中さん！</h2>
      <div class="card success">
        <p>✅ 出勤打刻：9:03（済み）</p>
      </div>
      <div class="card pending">
        <p>🕓 退勤打刻：未打刻</p>
      </div>
      <button class="btn-disabled" disabled>出勤する</button>
      <button class="btn-active">退勤する</button>
      <a class="footer-button" href="#">今日の打刻履歴を見る</a>
    </div>
    <%= yield %>
  </body>
</html>
